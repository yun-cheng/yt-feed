.venv/
__pycache__/
*.pyc
.env
node_modules/
dist/
data/*.db
data/*.db-shm
data/*.db-wal
data/downloads/
data/local_thumbs/
data/meili/
frontend/dist/
backend/config/*.json
backend/config/subscriptions.yaml
.env
.DS_Store

# Coverage output (vitest --coverage, python -m coverage)
frontend/coverage/
backend/.coverage

# Generated per-deployment secrets and adopted runtime config, on the data
# volume. `data/` is untracked today, but these must never become trackable:
# secret_key forges session cookies and the token files are live credentials.
data/secret_key
data/setup-token
data/config/

.venv/
__pycache__/
*.pyc
.env
node_modules/
dist/
data/*.db
data/*.db-shm
data/*.db-wal
data/downloads/
data/local_thumbs/
data/meili/
frontend/dist/
backend/config/*.json
backend/config/subscriptions.yaml
.env
.DS_Store

# Coverage output (vitest --coverage, python -m coverage)
frontend/coverage/
backend/.coverage

# Runtime config, now on the data volume rather than inside the repo. `data/` is
# untracked today, but this must never become trackable: it holds the OAuth
# token.
data/config/
